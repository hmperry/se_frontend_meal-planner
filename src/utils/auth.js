// POST /auth/register  → creates a new user in the DB, returns a JWT token
// POST /auth/login     → checks credentials, returns a JWT token
// GET  /auth/me        → verifies token, returns current user info
